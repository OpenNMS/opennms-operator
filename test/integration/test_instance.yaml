apiVersion: opennms.k8s.opennms.com/v1alpha1
kind: OpenNMS
metadata:
  name: test-instance
spec:
  version:
    distribution: Horizon
    tag: bleeding
  namespace: test-instance
  host: localhost
  allowedUsers: [ email@address.com ]
  disableProbes: true
  imageOverride:
    opennms: lipanski/docker-static-website:latest
    initContainer: alpine:latest
    grafana: lipanski/docker-static-website:latest
    auth: lipanski/docker-static-website:latest